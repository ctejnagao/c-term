# 機能開発指示: 社員提出の交通費等請求明細書（Excel）の現金出納取込機能

社員がExcelで作成・提出する「交通費等請求明細書」をC-TERPの現金出納メニューから取り込み、出金データ（旅費交通費等の精算）として登録する機能を実装してください。

---

## 1. 入力Excelのフォーマット仕様（解析対象）

アップロードされるExcel（.xlsx）のシート構造は以下の通りです。

- **社員名**: A1セル（または23行目の氏名欄）
- **対象年月**: B4:C4セル（例: `2025-07-01` 等のDateTime型または文字列）
- **明細テーブル開始行**: 5行目が見出し、6行目以降がデータ行
  - **B列（列インデックス1）**: 「日」（例: 3, 4, 7 または 1900-01-03 等の日付シリアル値）
  - **C列（列インデックス2）**: 「物件名 / 案件名」
  - **F列（列インデックス5）**: 「交通機関等」（高速、地下鉄、JR、タクシー、その他 等）
  - **H列（列インデックス7）**: 「摘要」（名二環、ガソリン代、桜通線 等）
  - **K列（列インデックス10）**: 「金額」（数値）
- **終了条件**: J列に「合計」と記載された行（17行目等）の手前までを明細として取得する。

### 重要: Excel特有の空欄補完ルール（フォワードフィル）
- 同一日の複数移動や、同一案件の複数交通機関の場合、**B列（日）やC列（物件名）が空欄**になります。
- 直前の行に値がある場合は、**前の行の「日」「物件名」を引き継いでデータを補完**してください。

---

## 2. 勘定科目・税区分の自動判定ルール

明細行の「交通機関等」および「摘要」から、弥生会計マスタに準拠した勘定科目を自動マッピングしてください。

1. **旅費交通費（課税仕入 10%）**
   - 交通機関等: `地下鉄`, `私鉄`, `JR`, `新幹線`, `タクシー`, `バス`, `高速`, `出張手当`
   - 摘要キーワード: `駐車代`, `コインパーキング`
2. **車両費（または旅費交通費）（課税仕入 10%）**
   - 交通機関等: `その他` かつ 摘要キーワード: `ガソリン`, `給油`
3. **消耗品費 / 雑費（課税仕入 10%）**
   - 交通機関等: `その他`, `社内備品購入`, `機器代金`, `書籍代金`

---

## 3. 実装要件

### A. バックエンド / 共通パーサー（`xlsx` ライブラリ利用）
- `xlsx` (SheetJS) を用いて、アップロードされたBufferから以下のJSON配列を生成する関数を実装してください。
```typescript
interface ExpenseReportItem {
  date: string;          // YYYY-MM-DD（対象年月 + 日）
  projectName: string;   // 物件名
  transportType: string; // 交通機関等
  description: string;   // 摘要
  amount: number;        // 金額
  accountCode: string;   // 推定勘定科目コード
  accountName: string;   // 勘定科目名（旅費交通費 等）
  taxType: string;       // 課税仕入 10% 等
}

interface ExpenseReportSummary {
  employeeName: string;  // 申請者氏名
  targetYearMonth: string;// YYYY-MM
  totalAmount: number;   // 合計金額
  items: ExpenseReportItem[];
}

### 物件名（プロジェクト）の照合・ハンドリング仕様
- Excelの「物件名」を既存のプロジェクトマスタと前方一致・部分一致で検索し、自動マッピングしてください。
- **マスタに該当する物件がない場合**:
  - 取込エラーにはせず、`projectId = null`（未紐付け / 一般管理費扱い）として処理してください。
  - 取込プレビューモーダル上で「未登録物件」として視覚的にアラートを表示し、画面上で既存プロジェクトを手動選択、または「物件指定なし」のまま登録できるようにしてください。
  